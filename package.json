{
    "name": "polskilogopeda",
    "description": "",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "deploy": "bundle exec jekyll build && git-directory-deploy --directory _site --branch gh-pages"
    },
    "devDependencies": {
        "git-directory-deploy": "1.5.1"
    }
}
